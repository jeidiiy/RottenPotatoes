import React from 'react';
import { ScrollView, View } from 'react-native';

const MovieProfile = () => {
  return (
    <View>
      <ScrollView>
      </ScrollView>
    </View>
  )
};

export default MovieProfile;